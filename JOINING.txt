SELECT ENAME,MGR
FROM EMP E,EMP;

SELECT*
FROM EMP E,EMP;

SELECT*
FROM EMP E,EMP M
WHERE E.MGR=M.EMPNO;

SELECT E.EMPNO,E.ENAME EMP_NAME,M.ENAME MGR_NAME
FROM EMP E,EMP M
WHERE E.MGR=M.EMPNO;

SELECT E.ENAME EMP_NAME,M.ENAME MGR_NAME
FROM EMP E,EMP M
WHERE E.MGR=M.EMPNO(+);

SELECT E.ENAME EMP_NAME,NVL(M.ENAME,'CEO') MGR_NAME
FROM EMP E,EMP M
WHERE E.MGR=M.EMPNO;

SELECT E.ENAME EMP_NAME,NVL(M.ENAME,'CEO') MGR_NAME
FROM EMP E,EMP M
WHERE E.MGR=M.EMPNO(+);

SELECT E.EMPNO,E.ENAME,E.DEPTNO,D.DNAME,D.LOC,D.DEPTNO
FROM EMP E,DEPT D
WHERE (E.DEPTNO=D.DEPTNO) AND (DNAME='ACCOUNTING' OR ENAME='KING')

SELECT E.EMPNO,E.ENAME,S.GRADE,S.LOSAL,S.HISAL
FROM EMP E,SALGRADE S
WHERE E.SAL BETWEEN S.LOSAL AND S.HISAL

SELECT*FROM EMP;


SELECT E.DEPTNO,E.ENAME,D.DNAME,D.DEPTNO
FROM EMP E, DEPT D
WHERE E.DEPTNO(+)=D.DEPTNO

SELECT worker.ename||' works for '||manager.ename
FROM emp worker, emp manager
WHERE worker.mgr=manager.empno








